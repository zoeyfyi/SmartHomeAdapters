language: generic

jobs:
  include:
    # test client server
    - stage: test
      script: eval "$(gimme 1.11)" && cd ${TRAVIS_BUILD_DIR}/clientserver && go get -v ./... && go test
    # test user server
    - stage: test
      script: eval "$(gimme 1.11)" && cd ${TRAVIS_BUILD_DIR}/userserver && go get -v ./... && go test
    # test robot server
    - stage: test
      script: eval "$(gimme 1.11)" && cd ${TRAVIS_BUILD_DIR}/robotserver && go get -v ./... && go test
